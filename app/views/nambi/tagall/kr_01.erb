<%= render :partial => "nambi/partials/header" %>
<%= render :partial => "nambi/partials/tagall" %>
            <div class="insight-Hashbox">
                <div id="tagall">
                  <!--여기에 버튼별 결과출력물이 뜨도록 --> 
                  <div id="kr-01">
                    <!-- 1. ㄱ으로 시작하는거: 세션됨--> 
                    <% @findkr01.each do |x| %>
                            <% tagpick = x.tags.where("tagName >= ? and tagName < ?", "가", "나") %>
                            <% tagpick.each do |t| %>
                                    <%= t.tagName %>  
                            <% end %>
                    <% end %>
                </div>      
                </div>
                <!-- 전체해시태그 box -->
            </div>